const sb=supabase.createClient(window.SUPABASE_URL,window.SUPABASE_ANON_KEY);const form=document.getElementById('registrationForm'),msg=document.getElementById('formMessage'),btn=document.getElementById('submitBtn');function show(t,e=false){msg.textContent=t;msg.className='message'+(e?' error':'');}form.addEventListener('submit',async e=>{e.preventDefault();btn.disabled=true;btn.textContent='Отправка...';const d=Object.fromEntries(new FormData(form).entries());const {error}=await sb.from('applications').insert([{name:d.name.trim(),course:d.course,specialty:d.specialty.trim(),brigade:d.brigade,contact:d.contact.trim(),goal:d.goal.trim(),experience:(d.experience||'').trim()}]);if(error){console.error(error);show('Не удалось отправить заявку. Проверьте настройки Supabase.',true);}else{form.reset();show('Заявка успешно отправлена. Спасибо!');}btn.disabled=false;btn.textContent='Подать заявку';});